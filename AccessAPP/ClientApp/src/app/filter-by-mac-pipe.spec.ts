import { FilterByMacPipe } from './filter-by-mac-pipe';

describe('FilterByMacPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMacPipe();
    expect(pipe).toBeTruthy();
  });
});
