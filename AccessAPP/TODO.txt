AccessApp Cassia backend:

MAJOR:
-> Implement function to get queuelist with mac and fw version
-> Implement function to get programminglist with mac and fw version
-> Implement mqtt status message to include current programming count as we did what is in queue
-> Implement mqtt cmd to set / get number of parrallel programmers


MINOR:
- Queue can be stuck if an error happens - make sure to handle that

- On error make sure to disconnect from sensor again!
        -> HAVE made sure the while loop can exit efter retry limit reached - to test!
